version: '3.9'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: watch_crm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '5432:5432'
    volumes:
      - ./db/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
